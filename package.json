{
  "name": "npm-project",
  "version": "0.0.1",
  "description": "Portfolio NPM Project",
  "main": "index.js",
  "scripts": {
    "css": "node-sass -o dist/css src/scss --output-style compressed --source-map-embed",
    "autoprefixer": "postcss -u autoprefixer --autoprefixer.browsers '> 5%, ie 9' -r dist/css/*",
    "build:css": "npm run css && npm run autoprefixer",
    "build:js": "babel src/js --out-dir dist/js",
    "deploy": "gh-pages -d dist",
    "watch:css": "onchange src/scss/*.scss -- npm run build:css",
    "watch:js": "onchange src/js/*.js -- npm run build:js",
    "watch:html": "onchange dist/*.html -- npm run serve",
    "serve": "browser-sync start --server dist --files dist",
    "dev": "parallelshell 'npm run watch:css' 'npm run watch:js' 'npm run serve'",
    "lint:sass": "sass-lint src/**/*.scss -c -v",
    "lint:js": "eslint src/js/**/*.js",
    "lint": "npm run lint:sass && npm run lint:js",
    "test": "mocha --compilers js:babel-register",
    "test:watch": "mocha --compilers js:babel-register --watch",
    "validate": "npm ls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pauurrutia118/pauurrutia-portfolio.git"
  },
  "keywords": [
    "npm",
    "boilerplate",
    "frontend"
  ],
  "author": "Pau U",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pauurrutia118/pauurrutia-portfolio.git"
  },
  "homepage": "https://github.com/pauurrutia118/pauurrutia-portfolio.git",
  "devDependencies": {
    "autoprefixer": "^6.3.6",
    "babel-cli": "^6.6.5",
    "babel-eslint": "^6.0.2",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.6.0",
    "browser-sync": "^2.11.2",
    "chai": "^3.5.0",
    "eslint": ">=4.18.2",
    "gh-pages": "^0.12.0",
    "mocha": "^2.4.5",
    "node-sass": "^3.4.2",
    "onchange": "^2.2.0",
    "parallelshell": "^2.0.0",
    "postcss-cli": "^2.6.0",
    "sass-lint": "^1.5.1"
  }
}
